{% extends sonata_block.templates.block_base %}

{% block block %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">{{ info.title }}</h3>
        </div>
        <div class="box-body">
            <table class="table table-hover">
                <tbody>
                    {% if info.badCategories|length > 0 %}
                        {% for categoryName, badCategory in info.badCategories %}
                            <tr>
                                <td class="sonata-ba-list-label" style="width: 25%;">
                                    {{ categoryName }}
                                </td>
                                <td class="sonata-ba-list-label">
                                    <a href="https://look4wear.ru/filter/{{ badCategory.category.alias }}/{{ badCategory.topVendorName }}" target="_blank">пример</a>
                                </td>
                                <td class="sonata-ba-list-label">
                                    {{ badCategory.topVendorCount }}
                                </td>
                                <td class="sonata-ba-list-label">
                                    {{ badCategory.realCount }}
                                </td>
                            </tr>
                        {% endfor %}
                    {% else %}
                        <tr>
                            <td class="sonata-ba-list-label" >
                                Какая красота! Несоответствий нет!
                            </td>
                        </tr>
                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}